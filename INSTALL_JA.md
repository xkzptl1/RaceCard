# ソースからRaceCardをインストール

1. Apple Silicon搭載Mac、macOS 15以降、Xcode 26.5以降、Python 3を用意します。
2. このソースリポジトリを取得し、そのフォルダで `./Scripts/build.sh` を実行します。初回は固定されたSwift依存パッケージを取得します。
3. `build/Products/RaceCard.app` を開きます。個人利用のためアプリケーションフォルダにコピーすることもできます。
4. オフラインデモにアカウントは不要です。実際の過去データは「セッション」から選択します。ライブ利用にはご自身のOpenF1利用権限が必要です。
5. 「設定 → 表示 → 言語」で英語・日本語を切り替えられます。

公開ソース版では、チームロゴの代わりに文字バッジを表示します。ドライバー画像は任意の実行時取得で、取得できない場合は汎用アバターになります。公式図面由来の表示レイヤーを除外し、記録されたXY座標による地図を利用します。

RaceCardは個人・非商用利用向けのソース公開（source-available）プロジェクトです。LICENSEの条件が適用され、商用利用は許諾されません。第三者のデータ・画像等の権利は別です。READMEとTHIRD_PARTY_NOTICESもご確認ください。公開配布用のビルド済みアプリは同梱していません。
